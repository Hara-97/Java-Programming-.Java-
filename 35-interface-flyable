// 35.	Create an interface Flyable with a method fly(). Make a Bird class implement Flyable and a Plane class implement Flyable. Demonstrate interface usage
// Define the Flyable interface
interface Flyable {
    void fly();
}

// Bird class implements Flyable
class Bird implements Flyable {
    public void fly() {
        System.out.println("Bird is flying using wings.");
    }
}

// Plane class implements Flyable
class Plane implements Flyable {
    public void fly() {
        System.out.println("Plane is flying using engines.");
    }
}

// Main class to demonstrate interface usage
public class Main {
    public static void main(String[] args) {
        Flyable bird = new Bird();
        Flyable plane = new Plane();

        bird.fly();
        plane.fly();
    }
}
