// 27.	Enhance the Car class (Q26) by adding a default constructor and a parameterized constructor that initializes all three instance variables. Create objects using both constructors
class Car {
    String make;
    String model;
    int year;

    // Default constructor
    Car() {
        make = "Not set";
        model = "Not set";
        year = 0;
    }

    // Parameterized constructor
    Car(String m, String mo, int y) {
        make = m;
        model = mo;
        year = y;
    }

    // Print details
    void show() {
        System.out.println("Make: " + make);
        System.out.println("Model: " + model);
        System.out.println("Year: " + year);
    }

    public static void main(String[] args) {
        // Using default constructor
        Car car1 = new Car();
        System.out.println("Car 1:");
        car1.show();

        // Using parameterized constructor
        Car car2 = new Car("Honda", "City", 2023);
        System.out.println("\nCar 2:");
        car2.show();
    }
}
