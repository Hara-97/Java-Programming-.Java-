// 20.	Write a program that takes an array of 5 integers as input, stores them, and then prints the sum of all elements in the array. 
import java.util.Scanner;

public class SumArray {
    public static void main(String[] args) {
        int[] a = new int[5];
        int sum = 0;
        Scanner sc = new Scanner(System.in);

        System.out.println("Enter 5 numbers:");
        for (int i = 0; i < 5; i++) {
            a[i] = sc.nextInt();
            sum = sum + a[i];
        }

        System.out.println("Sum = " + sum);
    }
}
